import { BookingstatusFilterPipe } from './bookingstatus-filter-pipe';

describe('BookingstatusFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BookingstatusFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
